export class Serie {

    constructor(public id: number, public titulo:string,  public canal:string, public temporadas:number, public link:string, 
        public descripcion:string, public imagen: string){
        
    }
  }